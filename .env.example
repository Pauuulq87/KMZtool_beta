# 環境變數模板（請依需求填寫，勿直接提交實際密鑰）

## 前端（Vite）
# Google Maps API Key：用於地圖與 Places，自行在 GCP 申請並限制網域/金鑰權限
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

## 後端（Node/Express/Prisma）
# PostgreSQL 連線字串範例：postgresql://帳號:密碼@主機:埠號/資料庫?schema=public
DATABASE_URL=postgresql://user:password@localhost:5432/waypointmap?schema=public

# JWT 設定：請改為隨機長字串；有效期可調整（例：7d）
JWT_SECRET=change-this-secret
JWT_EXPIRES_IN=7d

# 伺服器埠號（必要時調整）
PORT=3000
